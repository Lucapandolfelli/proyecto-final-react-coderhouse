const Products = () => {
  return (
    <main className="site-page">
      <section className="section container">
        <h1>Products</h1>
      </section>
    </main>
  );
};

export default Products;
